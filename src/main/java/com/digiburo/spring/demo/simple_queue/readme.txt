Demonstrate the simplest use of JMS queue using Spring.

SimpleQueueDriver writes a String message which is converted to TextMessage and delivered to SimpleListener.

Message flow is one way, there is no return message.